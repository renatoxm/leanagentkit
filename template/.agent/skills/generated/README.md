# Generated Skills

Project-specific artifact generators authored by `leanagentkit-skill-artifact-template`. Each
is self-contained: it + its recipe in `.agent/recipes/` is enough to create the
artifact without reading the whole codebase.

Invoke: **"Read `.agent/skills/generated/leanagentkit-create-<type>.md` and follow it."**

| Generator | Artifact | Recipe | Authored |
|-----------|----------|--------|----------|
| _(none yet — run `leanagentkit-skill-artifact-template` to author one)_ | | | |

`_GENERATOR_TEMPLATE.md` is the blueprint cloned for each new generator — don't run it directly.
