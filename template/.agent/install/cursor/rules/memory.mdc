---
description: Lean Agent Kit memory protocol — follow AGENTS.md every session
alwaysApply: true
---

Always follow `AGENTS.md`, especially section 6 (Memory protocol).
At task start read `docs/memory/ACTIVE_CONTEXT.md` and `docs/CODEBASE_MAP.md`.
Do not glob the repo — use the map to open only relevant files.
