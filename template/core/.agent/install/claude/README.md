# Claude Code install templates

Copied/used by `leanagentkit-wire-agent`:

- `CLAUDE.md` → project root (pointer to `AGENTS.md` §6)

Skill wrappers are **generated** under `.claude/skills/` from
`.agent/skills/leanagentkit-*.md` present on disk (core + installed packs).
Re-run wire-agent after `--enable-pack`.
