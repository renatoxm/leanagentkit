# CLAUDE.md

Follow `AGENTS.md`, especially section 6 (Memory protocol).
At task start read `docs/memory/ACTIVE_CONTEXT.md` and `docs/CODEBASE_MAP.md`.
Map-first: open named files; use narrow search when the map is insufficient.
Do not dump the whole repo to re-orient. Match workflow size (trivial / normal / substantial).
