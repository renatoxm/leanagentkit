# CLAUDE.md

Follow `AGENTS.md`, especially section 6 (ambient memory protocol).
At task start read `docs/memory/ACTIVE_CONTEXT.md`, `docs/CODEBASE_MAP.md`, and skim
Open entries in `docs/memory/LEARNINGS.md`.
Map-first: open named files; use narrow search when the map is insufficient.
Refresh ACTIVE_CONTEXT on focus/file milestones — not only at session end.
On avoidable failures, append or bump LEARNINGS. Do not dump the whole repo to re-orient.
Match workflow size (trivial / normal / substantial).
