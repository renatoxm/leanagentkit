{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "prompt",
        "prompt": "Lean Agent Kit session: Follow AGENTS.md section 6 (ambient memory). At task start read docs/memory/ACTIVE_CONTEXT.md, docs/CODEBASE_MAP.md, and skim Open entries in docs/memory/LEARNINGS.md before globbing the repo. leanagentkit-start-session is optional (pack hooks). Do not run bootstrap or other setup unless the user asks.",
        "timeout": 30
      }
    ],
    "sessionEnd": [
      {
        "type": "prompt",
        "prompt": "Lean Agent Kit session: If meaningful work happened, refresh docs/memory/ACTIVE_CONTEXT.md Resume if stale. If avoidable hook/lint failures were fixed this session, append or bump docs/memory/LEARNINGS.md. For finalize with packs/PROGRESS, tell the user to run leanagentkit-end-session in chat. Do not auto-commit or auto-push.",
        "timeout": 30
      }
    ]
  }
}
