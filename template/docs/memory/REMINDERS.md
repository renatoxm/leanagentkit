# Reminders

> Personal nudges managed by Trevor (`leanagentkit-ask-trevor`).
> Not for project blockers — use `ACTIVE_CONTEXT.md` open questions for those.
> Entries are never deleted; mark `status: done` when finished.
>
> Enable session reminders: copy `.leanagentkit/trevor.yml.example` to
> `.leanagentkit/trevor.yml` and set `enabled: true`.

<!--
Schema (one block per reminder):

## R-001 · Short title
- created: YYYY-MM-DD
- show_after: YYYY-MM-DD        # optional — snooze until this date
- status: pending | acknowledged | done
- acknowledged: YYYY-MM-DD      # set when user acknowledges
- linked: docs/specs/NNN-feature.md   # optional
- backlog: BACK-12              # optional — Backlog card id
- note: one-line detail
-->

<!-- Add reminders below. IDs must be monotonic (R-001, R-002, …). -->
